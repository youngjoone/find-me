DB_URL=jdbc:postgresql://db-auth:5432/auth
DB_USER=auth
DB_PASS=authpass
JWT_ISSUER=http://auth.local
JWT_SECRET=YOUR_JWT_SECRET_HERE # Generate a secure base64 encoded string (e.g., openssl rand -base64 32)
JWT_PRIVATE_KEY_PEM=-----BEGIN PRIVATE KEY-----... (dev only)
